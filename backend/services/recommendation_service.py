def get_recommendations(budget, preferences):
    # Placeholder for API integration (e.g., Booking.com API)
    # For now, return mock data
    return {
        "hotels": ["Hotel A", "Hotel B"],
        "restaurants": ["Restaurant X", "Restaurant Y"]
    }